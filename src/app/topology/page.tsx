import { TopologyWorkspace } from "@/components/topology/topology-workspace";

export const metadata = {
  title: "Network Topology Lab | Network Visualization Platform",
  description: "Design, configure, validate, and simulate computer networks visually.",
};

export default function TopologyPage() {
  return <TopologyWorkspace />;
}
